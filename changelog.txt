v0.4
- Custom tracks have a delete button (the bin). Click a row to play that song
- Change logo uses the Terraria tree icon, sized for the wrench circle
- This mod's row in the Mods menu uses the workshop gif as its background, with a #0099ff outline
- New moon-phase widget (vanilla moon frames, layout-editable)
- Borrowed skies are labeled live vs still. Collapsed player shows the current track on hover

v0.3
- Borrowed skies run the other menu's own draw (animations, shaders, clouds). Stills are only list previews, never a live substitute
- Wrath of the Gods: Ascent uses Terminus stairway sky; Carmine Yonder uses the rift sky target (not a stretched thumbnail or flat gradient)
- Catalyst: Astrageldon wallpaper is the live menu background; the CATALYST wordmark is the logo slot (menu-theme logo, not a boss icon). Point-sampled so it does not grow a halo
- Logo and sky stay independent: wallpaper pass skips that theme's logo, logo pass skips the sky

v0.2
- Safer previews for borrowed skies (still images from the other mod, never a live render-target capture)
- Wrath of the Gods: Ascent listed with a map still; Carmine Yonder draws via their sky target instead of stretching a tiny shader thumbnail
- Catalyst: Astrageldon wallpaper and CATALYST wordmark are separate; wallpaper no longer stamps the logo
- Paradise's Shining Backdrop removed from borrow lists (its sky only exists while that WotG theme is selected)
- Music player uses the Play/Pause/Prev/Next/Playlist/Upload/Shuffle/Loop icons
- Removed the Both shortcut so logo and sky stay independent

v0.1
- First public build: vanilla-looking title theme with wrench customize hub, wallpaper layers, music player, widgets, layout editor, and client window chrome
