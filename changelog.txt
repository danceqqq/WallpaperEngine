v0.8.7
- GIFs actually decode: the palette reader was advancing a copy of the file cursor, so every GIF fell back to a still first frame. No extra libraries.

v0.8.6
- GIF playback: swap frames on a free texture (they stayed frozen on frame 1)
- Wallpaper panel grouped: sky / layers / your images. Layer settings sit on the selected layer. Trash removes it (the old Remove button was clicking the file list instead)
- Hash-looking filenames show as GIF · 3acd2f6a

v0.8.5
- GIF / animated PNG actually play on the menu (they were stuck on the first frame after import)
- Remove layer works; trash on the layer row too. Last image layer falls back to vanilla sky

v0.8.4
- GIF and animated PNG as wallpapers, logos, and image layers

v0.8.3
- Client settings grouped into cards with live previews (hub, window chrome, menu buttons, accent)
- Import your own TTF/OTF menu fonts; width and height sliders; button styles apply to custom fonts too

v0.8.2
- Change logo: toggle the grow/shrink bounce so the logo stays on screen
- Music: mute the custom player when Terraria is in the background
- Client: menu text color, font picker, button outline/plate styles
- Client: save and load Looks (theme presets). Right-click a look to delete it

v0.8.1
- Custom menu music keeps playing on Characters and Worlds
- Custom player actually feeds audio again on the title screen (it could sit at 0:00 after the last music fix)
- Imported wallpapers sit in the UI center; Change wallpaper has Center image. Pan works for Fit inside too
- Change logo includes Watermelon 1, Watermelon 2, and Blue Archive Style without other mods

v0.8
- Holding player / hub / panel buttons no longer repeats like an autoclicker. One press, one action until you release

v0.7
- Discord wide banner lists voice channels (with Join) and scrolls when they do not fit. Member list puts a Join pill on anyone in voice
- Discord cards use an accent edge, header rule, and channel grouping instead of a flat grey box
- Widgets panel: live-preview tiles. Hover an active tile to slide its settings open; the list below moves and scroll follows
- Open customize panels no longer click through into the hub, widgets, or menu buttons underneath (picking a wallpaper will not also open Music)
- tModLoader / Terraria social icons sit at their native menu spots again (bottom-left / top-left). Old top-right layout saves are cleared
- Discord voice line follows occupied members, not the first channel in widget.json. Polls a bit faster and skips HTTP cache
- Player widget next / previous / pause / loop / shuffle register one click (they were double-toggling and looked dead). Loop still repeats the current track; shuffle is used when you skip or when a track ends

v0.6
- Borrowed Catalyst logos use Terraria's own logo bounce (rotation + scale)
- Wrath of the Gods "The Insouciant" wallpaper draws the live carmine rift sky. Scoped menu swap is restored every frame
- Discord server widget: paste the server ID in the widgets panel (no discord.txt). Compact / banner / member-list. Live previews. Refresh about every 15s. Server icon from the widget invite. Voice members sort first. Join Voice opens the Discord app when it is installed

v0.5
- Client settings can switch the customize hub between the circle and a sliding iOS-style bar (live previews in the panel)
- Menu buttons: visuals and hitboxes stay together after moving them. Thanks Glorp for the report

v0.4
- Custom tracks have a delete button. Click a row to play that song
- Change logo uses the Terraria tree icon, sized for the wrench circle
- This mod's row in the Mods menu uses the workshop gif as its background
- Moon-phase widget (vanilla moon frames, layout-editable)
- Borrowed skies are labeled live vs still. Collapsed player shows the current track on hover

v0.3
- Borrowed skies run the other menu's own draw (animations, shaders, clouds). Stills are only list previews
- Wrath of the Gods: Ascent uses Terminus stairway sky; Carmine Yonder uses the rift sky target
- Catalyst: Astrageldon wallpaper is the live menu background; the CATALYST wordmark is the logo slot. Point-sampled so it does not grow a halo
- Logo and sky stay independent: wallpaper pass skips that theme's logo, logo pass skips the sky

v0.2
- Safer previews for borrowed skies (still images, never a live render-target capture)
- Paradise's Shining Backdrop removed from borrow lists
- Music player uses Play/Pause/Prev/Next/Playlist/Upload/Shuffle/Loop icons
- Removed the Both shortcut so logo and sky stay independent

v0.1
- First public build: vanilla-looking title theme with wrench customize hub, wallpaper layers, music player, widgets, layout editor, and client window chrome
